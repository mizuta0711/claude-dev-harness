# tools/（Phase 2 で作成）

ハーネス自体のツール置き場。

予定:

| ファイル | 用途 |
|---------|------|
| `create-project.mjs` | `templates/base` + `templates/<env>` を合成して新規プロジェクトを生成する（プレースホルダ置換・dry-run 対応。WPF 版 `init-template.ps1` の Node 移植） |

現時点では空。Phase 2 で作成する。
