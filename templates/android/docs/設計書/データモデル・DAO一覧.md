# データモデル・DAO 一覧

## 概要

永続化しているデータの実態。Room のエンティティ・DAO と、DataStore のキーを扱う。

- 関連方針: [.claude/rules/android-data.md](../../.claude/rules/android-data.md)

実装状況の凡例: ✅ 実装済 / 🔵 設計済（未実装） / ⚪ 将来フェーズ

> このファイルは「実態の一覧」です。実装の追加・変更時に `/harness-core:update-docs` で同期します。
> 初期状態は空です。最初のエンティティ・設定キーを実装したら下表に追記してください。

---

## Room エンティティ

| エンティティ | テーブル | 主キー | 一意制約・インデックス | 実装状況 |
|------------|---------|-------|---------------------|---------|
| <!-- 例: MessageEntity --> | <!-- messages --> | <!-- id --> | <!-- (threadId, sentAt) --> | 🔵 |

<!-- 列の定義はエンティティごとに節を分けて書く（例）

### MessageEntity

| 列 | 型 | Null | 説明 |
|----|----|------|------|
| `id` | `Long` | No | 主キー（自動採番） |

-->

## DAO

| DAO | 主なクエリ | 戻り値の形 | 備考 |
|-----|----------|-----------|------|
| <!-- 例: MessageDao --> | <!-- observeByThread --> | <!-- Flow<List<...>> --> | <!-- 購読用 --> |

## DataStore のキー

| キー | 型 | 既定値 | 用途 |
|------|----|-------|------|
| <!-- 例: theme --> | <!-- String --> | <!-- "system" --> | <!-- テーマ設定 --> |

## マイグレーション

| バージョン | 変更内容 | 対応 |
|-----------|---------|------|
| <!-- 1 → 2 --> | <!-- 列を追加 --> | <!-- Migration(1,2) --> |

> **`fallbackToDestructiveMigration()` を使ったら、その旨と理由をここに書く**
> （利用者のデータが消えるため、暗黙に採用しない）。

---

## 改訂履歴
| 版数 | 日付 | コミット | 内容 | 担当 |
|------|------|---------|------|------|
| 1.0 | yyyy-mm-dd | (テンプレート) | 初版（空スケルトン） | |
